#include <iostream> // # is used to includde header file like libraries

int main() // A program starts from here it accts as a entry point, this is 'main; part of the program
// int is used to ensure the output in integral form.
{
    // std is part of header file, in which C out is function 
    std::cout << "Hello, jvuyv nnkivuWorld!";

    return 0; // this means succesful termination of the program
}
